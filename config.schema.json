{
  "pluginAlias": "SwitchBotCustomHumidifier",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "required": true,
        "default": "SwitchBot Custom Himidifier"
      },
      "token": {
        "title": "SwitchBot Token",
        "type": "string",
        "required": true,
        "description": "Generated token in SwitchBot app (Revealed by tapping 10 times on version in settings)"
      },
      "mapping": {
        "title": "Key mapping",
        "type": "object",
        "properties": {
          "on": {
            "title": "ON",
            "type": "string",
            "required": true,
            "description": "Power ON button (If the button is toggle, assign same button as off)"
          },
          "off": {
            "title": "OFF",
            "type": "string",
            "required": true,
            "description": "Power OFF button (If the button is toggle, assign same button as on)"
          }
        }
      }
    }
  }
}
